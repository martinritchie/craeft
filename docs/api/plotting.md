# Plotting

Matplotlib-based visualisation utilities. All functions return a `Figure`
object — the caller controls output (display, save, etc.).

Requires the `plot` optional dependency:

```bash
uv add "craeft[plot]"
```

::: craeft.utils.plotting
    options:
      members:
        - plot_sir
        - plot_trajectories
        - plot_prevalence_comparison
